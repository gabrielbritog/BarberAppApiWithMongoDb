/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { MiscConditionsDirective } from './MiscConditions.directive';

describe('Directive: MiscConditions', () => {
  it('should create an instance', () => {
    const directive = new MiscConditionsDirective();
    expect(directive).toBeTruthy();
  });
});
