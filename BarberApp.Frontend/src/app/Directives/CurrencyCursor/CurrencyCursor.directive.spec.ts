/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { CurrencyCursorDirective } from './CurrencyCursor.directive';

describe('Directive: CurrencyCursor', () => {
  it('should create an instance', () => {
    const directive = new CurrencyCursorDirective();
    expect(directive).toBeTruthy();
  });
});
