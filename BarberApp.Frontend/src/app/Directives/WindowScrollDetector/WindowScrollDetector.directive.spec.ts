/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { WindowScrollDetectorDirective } from './WindowScrollDetector.directive';

describe('Directive: WindowScrollDetector', () => {
  it('should create an instance', () => {
    const directive = new WindowScrollDetectorDirective();
    expect(directive).toBeTruthy();
  });
});
